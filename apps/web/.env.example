# Keep /api when the frontend and API share an origin in production.
VITE_API_URL=/api
# Leave blank for same-origin Socket.IO. Set a full origin when hosted separately.
VITE_SOCKET_URL=
VITE_DEV_API_TARGET=http://localhost:4000
VITE_DEV_SOCKET_TARGET=http://localhost:4000
WEB_PORT=5173
